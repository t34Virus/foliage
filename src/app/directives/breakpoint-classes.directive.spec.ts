import { BreakpointClassesDirective } from './breakpoint-classes.directive';

describe('BreakpointClassesDirective', () => {
  it('should create an instance', () => {
    const directive = new BreakpointClassesDirective();
    expect(directive).toBeTruthy();
  });
});
